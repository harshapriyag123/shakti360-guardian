# Video shotlist

Record at 1920×1080 or higher with a large terminal font and readable Markdown preview. Use the [demo script](DEMO_SCRIPT.md) for exact commands. Capture genuine output; do not create replacement screenshots or alter status values.

| Shot | Capture instructions | Keep visible |
|---|---|---|
| Public registry | Open the pinned public page in a clean browser window. Close unrelated tabs, bookmarks and account menus. | Project name, `0.1.0`, public URL and description. |
| Inspection | Run inspection; crop to the Play identity and eligibility. Separately show the usage disclosure or presentation. | Python requirement, process-policy caveat and actual network/file effects. |
| Five-stage run | Capture Houston execution from launch to completion. Crop private output paths. Condense waits only with a caption. | Resolve, weather, air, alerts, compose and actual statuses. |
| Markdown brief | Open the real session's `houston/brief.md` in preview. Hide editor explorer, title-bar paths and recent-file menus. | Destination, time window, source timestamps, baseline creation and limitations. |
| Baseline changes | Open `houston-refresh/brief.md`. Use the same outing context. | Comparison text, including “No changes” if that is the result. |
| Degraded coverage | Preview Chennai output. | Unsupported official alerts, available source status, no-all-clear caveat. |
| Failure demonstration | Run offline demo, then preview `03-source-failure.md`. | Synthetic banner, timeout reason, STALE data and timestamp; do not crop away the synthetic label. |
| Journey/DAG | Capture the verified installed local Play's dry-run dependency plan plus an actual completed ledger. | Three levels with parallel weather/air/alerts. Label plan versus execution. |
| Closing | Return to pinned public page or a plain title card with the exact URL. | Project impact and concise limitations. |

Use a neutral terminal prompt before recording. Avoid emails, tokens, local usernames, private filesystem paths, shell history, credentials, notifications, authenticated account menus and private viewer links. Inspect JSON, raw receipts and browser developer tools can contain private metadata; do not show them unreviewed. Record only generic city-level destinations. Public project owner `harshapriyag123` is part of the intentionally public URL.

Review frames during transitions as well as the final edit. Crop or blur private metadata without hiding data provenance, failures, timestamps or safety labels. Keep an unedited local capture for checking the edit, subject to your own retention decision; no automatic deletion is provided.
