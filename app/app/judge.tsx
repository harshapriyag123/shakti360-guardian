import { Text, View } from "react-native";
import { Card, colors, Eyebrow, Pill, Screen, Title } from "../lib/ui";
const layers = [["USER", "Explicit choices, consent, SafeWord"], ["ON-DEVICE LAYER", "Battery, permissions, temporary state"], ["GUARDIAN COORDINATOR", "Combines bounded signals"], ["POLICY ENGINE", "Deterministic, versioned decisions"], ["ACTION LAYER", "Trusted Circle • Resources • Evidence"]];
const events = ["Journey created", "Adaptive tracking selected", "Check-in scheduled", "Missed check-in simulated", "Verification policy applied"];
export default function Judge() { return <Screen><Eyebrow>JUDGE MODE • DEMO DATA</Eyebrow><Title subtitle="The invisible safety engineering, made visible.">How Shakti360 decides</Title><Card tone="dark"><Pill label="Human controlled" /><Text style={{ color: "white", fontSize: 21, fontWeight: "900" }}>AI assists. Policy decides. You act.</Text><Text style={{ color: "#C9DED6", lineHeight: 21 }}>The coordinator cannot call police. Every escalation ends by presenting explicit options.</Text></Card><View style={{ gap: 7 }}>{layers.map(([name, copy], i) => <View key={name} style={{ alignItems: "center" }}><Card style={{ width: "100%" }}><Text style={{ color: colors.primary, fontWeight: "900", fontSize: 12 }}>{name}</Text><Text style={{ color: colors.ink, fontWeight: "700" }}>{copy}</Text></Card>{i < layers.length - 1 ? <Text style={{ color: colors.primary, fontSize: 20 }}>↓</Text> : null}</View>)}</View><Card><Text style={{ color: colors.ink, fontWeight: "900", fontSize: 18 }}>90-second demo event stream</Text>{events.map((event, i) => <View key={event} style={{ flexDirection: "row", gap: 10 }}><Text style={{ color: colors.muted, fontVariant: ["tabular-nums"] }}>00:{String(i * 8 + 2).padStart(2,"0")}</Text><Text style={{ color: colors.ink, flex: 1 }}>{event}</Text></View>)}</Card><Text style={{ color: colors.muted, fontSize: 12 }}>Timeline is an explicitly labeled demo script. Pilot metrics elsewhere come from live session events.</Text></Screen>; }
