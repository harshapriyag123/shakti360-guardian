import { useState } from "react";
import { Link, router } from "expo-router";
import { Pressable, Text, View } from "react-native";
import { authPost } from "../lib/api";
import { Action, Card, colors, ErrorBanner, Eyebrow, Field, Loading, PasswordField, Screen, Title } from "../lib/ui";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[remember,setRemember]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[notice,setNotice]=useState("");async function submit(){try{setBusy(true);setError("");setNotice("");await authPost("/auth/login",{email,password,remember_me:remember});router.replace("/");}catch(e){setError(e instanceof Error?e.message:"Sign in failed");}finally{setBusy(false);}}async function forgot(){try{setBusy(true);setError("");await authPost("/auth/forgot-password",{email});setNotice("If an account exists, reset instructions will be sent. Email delivery is not configured in hackathon mode.");}catch(e){setError(e instanceof Error?e.message:"Request failed");}finally{setBusy(false);}}return <Screen><Card style={{width:"100%",maxWidth:480,alignSelf:"center",padding:28}}><View style={{width:58,height:58,borderRadius:19,backgroundColor:colors.mint,alignItems:"center",justifyContent:"center"}}><Text style={{fontSize:28}}>🛡️</Text></View><Eyebrow>SHAKTI360</Eyebrow><Title subtitle="Your safety setup is waiting for you.">Welcome back</Title><Field label="Email" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" autoComplete="email"/><PasswordField label="Password" value={password} onChangeText={setPassword} autoComplete="current-password"/><View style={{flexDirection:"row",justifyContent:"space-between",alignItems:"center",gap:12,flexWrap:"wrap"}}><Pressable accessibilityRole="checkbox" accessibilityState={{checked:remember}} onPress={()=>setRemember(v=>!v)} style={{minHeight:44,justifyContent:"center"}}><Text style={{color:colors.ink}}>{remember?"☑":"☐"} Remember me</Text></Pressable><Pressable onPress={forgot} style={{minHeight:44,justifyContent:"center"}}><Text style={{color:colors.primary,fontWeight:"800"}}>Forgot password?</Text></Pressable></View><Action label="Sign in securely" icon="log-in" onPress={submit} disabled={busy||!email||!password}/>{busy?<Loading label="Signing in securely…"/>:null}{notice?<Card tone="mint"><Text style={{color:colors.primaryDark,lineHeight:20}}>{notice}</Text></Card>:null}{error?<ErrorBanner message={error}/>:null}<Link href="/register" asChild><Pressable style={{minHeight:44,justifyContent:"center"}}><Text style={{color:colors.primary,textAlign:"center",fontWeight:"800"}}>New to Shakti360? Create an account</Text></Pressable></Link><Link href="/explore" asChild><Pressable style={{minHeight:44,justifyContent:"center"}}><Text style={{color:colors.muted,textAlign:"center",fontWeight:"700"}}>Explore without an account</Text></Pressable></Link></Card></Screen>}
